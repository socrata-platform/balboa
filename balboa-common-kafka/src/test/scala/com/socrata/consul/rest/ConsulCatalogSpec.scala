package com.socrata.consul.rest

import org.scalatest.WordSpec

/**
 * Attempts to
 */
object ConsulCatalogSpecSetup {

//  trait LocalCatalog {
//    val catalog = ConsulCatalog.cons("10.0.1.35") match {
//      case Some(c) => c
//      case None => throw new IllegalStateException("Unable to create Consul Catalog for test")
//    }
//  }
}

/**
 * Runs Consol Catalog Spec
 */
class ConsulCatalogSpec extends WordSpec {

//  "A Consul Catalog Rest Client" should {
//
//    "successfully query for a service that does not exists and return empty list" in new LocalCatalog {
//      catalog.servicesById(Some("catsrv"))
//    }
//
//  }

}
